# Discord Bot Configuration
# REQUIRED: Get your bot token from https://discord.com/developers/applications
DISCORD_TOKEN=your_discord_bot_token_here

# Environment (REQUIRED for production)
# Options: development, production
NODE_ENV=production

# Server Configuration (REQUIRED for Render/Docker)
# Port for health check server
PORT=3000

# Logging Level (Optional)
# Options: error, warn, info, debug
LOG_LEVEL=info

# Database Configuration (Optional)
# SQLite database file location - defaults to ./src/ragabot.db
DB_PATH=./src/ragabot.db

# Bot Settings (Optional - defaults work fine)
DEFAULT_PREFIX=!
DEFAULT_VOLUME=50
MAX_QUEUE_SIZE=500

# Owner Configuration (Optional - for admin commands)
# Add your Discord user ID(s) separated by commas
OWNER_IDS=your_discord_user_id_here

# Lavalink Configuration (NOT USED - Bot uses DisTube)
# These are legacy settings and not required
# LAVALINK_HOST=localhost
# LAVALINK_PORT=2333
# LAVALINK_PASSWORD=youshallnotpass

# Performance Settings (Optional)
MAX_MEMORY_RESTART=1G
AUTO_RESTART=true

# ================================
# DEPLOYMENT NOTES:
# ================================
# For Render.com deployment:
# 1. Set DISCORD_TOKEN in Render dashboard (Environment tab)
# 2. Set NODE_ENV=production
# 3. Set PORT=3000
# 4. Set DB_PATH=/data/ragabot.db (for persistent disk storage)
# 5. Add 1GB persistent disk mounted at /data
# 6. All other variables are optional
# 
# For local development:
# 1. Copy this file to .env
# 2. Add your DISCORD_TOKEN
# 3. Set NODE_ENV=development
# ================================